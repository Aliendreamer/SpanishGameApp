// Conventional Commits, checked by the commit-msg hook. The changelog is generated from these
// messages, so the type and any BREAKING CHANGE must be accurate.
module.exports = { extends: ['@commitlint/config-conventional'] };
