# Swipe deck: four parts built autonomously, report at the end

On 2026-09-23 the user approved splitting the swipe deck into four parts. Build them in order, each as its own OpenSpec change with TDD, reviews, gates and a commit, and report once at the end:
1. Tabs and card
2. Gestures and motion
3. Game rules and progress
4. "It's a match!"

The user's words: "yes do it in batches with tests but tell me at the end when all 4 are done".

**How to apply:**
- Make design calls yourself, following the handoff (`mem:design/swipe-game-ui-handoff`) and the roadmap.
- Record each call in the change's design.md, and list them in the final report.
- Don't stop between parts to ask.
- This overrides `mem:workflow/design-together` for these four parts only.
- Never push.
