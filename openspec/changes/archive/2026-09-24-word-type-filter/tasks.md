## 1. Setting

- [x] 1.1 Failing tests: migration 3 adds `word_type` (default all) and keeps settings; `wordType` saves and reads; implement

## 2. Deck

- [x] 2.1 Failing tests: `getDeck` deals only the chosen type; all types with All words; implement
- [x] 2.2 Failing tests: `WORD_TYPES` labels and counts from the stats; `levelLine` names the type; implement

## 3. Settings and route

- [x] 3.1 Failing tests: the WORD TYPE section shows the four options and saves a choice; implement
- [x] 3.2 Failing route test: choosing Verbs in Settings makes the Swipe tab deal verbs with "· Verbs" in the header
- [x] 3.3 Gates and Android bundle

## 4. From review

- [x] 4.1 Empty-state text covers swiped words and the word type ("You've already swiped every word for these settings. Try another level or word type, or include known words.")
